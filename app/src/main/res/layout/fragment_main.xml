<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="layout"
            type="com.ctyeung.skeletonex.MainFragment" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        tools:context=".MainFragment">

            <Button
                android:id="@+id/btnThin"
                android:layout_width="300dp"
                android:layout_height="wrap_content"
                android:text="Gonzales Thinning"
                android:layout_margin="20dp"
                android:onClick="@{()->layout.onClickThin()}"/>

            <Button
                android:id="@+id/btnDisMap"
                android:layout_width="300dp"
                android:layout_height="wrap_content"
                android:text="Distance Map"
                android:layout_margin="20dp"
                android:onClick="@{()->layout.onClickDisMap()}"/>

            <Button
                android:id="@+id/btnPhoto"
                android:layout_width="300dp"
                android:layout_height="wrap_content"
                android:text="Photo"
                android:layout_margin="20dp"
                android:onClick="@{()->layout.onClickPhoto()}"/>

    </LinearLayout>
</layout>